[project]
name = "fc-sentiment-analysis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Welepy-dev", email = "mwsilva.contato@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
fc-sentiment-analysis = "fc_sentiment_analysis:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
